# Elevate design system
## Latest chat correction — 7 September 2026

**This correction supersedes earlier navigation and layout descriptions.** Primary navigation has six desktop destinations: **Automation Centre, Sessions, Programmes, Drivers, Training library and Driver app**. At **680px and below**, the bottom bar keeps Automation Centre, Sessions, Programmes and Drivers, with **More** exposing Training library and Driver app. Driver app is a required clickable destination, not a route-only page; preserve the user’s KEEP markers in `dist/index.html` and the instructions in `AGENTS.md`. **Drivers contains Drivers / Groups tabs**; Programmes contains **Activity / Learning / Configuration / Automation**. Groups and its existing filters, charts and drawer belong to Drivers. Activity joins **weekly coaching at 70% and drivers needing attention at 30%** into one desktop row with **zero gutter and a single divider**. The full attention list scrolls independently; its count sits inline to the right of the heading. The joined panels share their top and bottom edges and stack on narrow screens. The selected programme’s KPI and weekly-chart score label is **`<programme name> score`** (for example, “Speeding score”); All programmes keeps **“Elevate score”**. The separate selected-programme event-rate Trend/Comparison card is removed. All programmes retains its Coaching/Event rates comparison and the underlying recorded rates, source endpoints and equivalent data. Keep shared typography and right-aligned scope controls.

The [full request audit](docs/request-audit-2026-09-07.md) distinguishes completed UI from pending score/evaluation decisions. Session creation origin is immutable; current delivery mode can become one-on-one after escalation. Visible method totals and filters follow delivery mode. Programme policy and video-library edits are local prototype controls; configuration preview does not run an evaluator. The two-week calendar default applies to fresh settings. Missing driver programme scores stay unavailable.

Session creation uses the same right-side, full-height 1060px drawer as session review, with fixed header actions and one scrolling body. Ask only for **Driver and Programme**. Show **Coach** as the current manager, read-only; manual creation does not use programme coach routing. There are no per-session Due, Training lesson or Reason inputs during creation. The saved global **Session due period** in Programmes → Automation applies to new sessions; its draft must not affect creation and existing deadlines remain unchanged. Matching videos already assigned to the driver and programme are selected automatically with all camera clips. Evidence sits below Coach in a compact table; previews start closed. Expanding a row shows video and location side by side, stacked on narrow screens. Add events opens an inline searchable picker inside the drawer. Add selected stages its choices; Cancel or Escape discards picker changes without closing creation. Unassigned evidence still requires explicit selection; creating commits links, cancelling changes nothing. Keep the source page and return context. Session review uses compact **Events / Videos** counts, no Messages count, a behaviour-named collapsed breakdown and a taller Conversation area. See the [session drawer reference review](docs/session-drawer-review.md).


The governing specification is [Elevate Autocoach — Design Library v1.1](Elevate-Autocoach-Design-Library.md), dated September 7, 2026. Read it before any UI work. This file supplements that library with repository integration and product behavior; it does not define a competing palette or component system. Current explicit user decisions take precedence.

The library replaces the former cobalt palette, font downloads, tinted panels, decorative gradients, aliased root tokens, status-as-method conflation, filled view tabs, and separate KPI/table styles. Earlier screenshots and concept pages under `docs/` are historical references only.

**September 7, 2026 governing navigation amendment.** Programmes combines Analytics and Programs into **Activity, Learning, Configuration and Automation** tabs. Drivers is a standalone destination containing Drivers and Groups. Primary navigation has six desktop destinations: **Automation Centre, Sessions, Programmes, Drivers, Training library and Driver app**. At **680px and below**, the bottom bar keeps Automation Centre, Sessions, Programmes and Drivers, with **More** exposing Training library and Driver app. Driver app is a required clickable destination, not a route-only page; preserve the user’s KEEP markers in `dist/index.html` and the instructions in `AGENTS.md`. This supersedes the earlier navigation recorded in [the meeting log](docs/meeting-2026-09-07-ui-decisions.md). Activity brings the existing summary, weekly chart, attention, programme comparison and retained outcomes together without a second session ledger. Configuration and fleet-wide Automation retain their current behavior. The earlier merge’s validation is recorded below as historical evidence; it does not establish validation of this latest correction.

A session still shows exactly one of In progress, Replied, Overdue, Completed or Repeated, with Archived as history. Needs review names the queue grouping Overdue, Repeated and Replied. The session workspace, driver portfolios, group drawers, local programme configuration and linked Driver app remain governed by their existing contracts. The navigation merge does not change scoring, escalation, delivery or execution behavior.

## Sources of truth

| File | Responsibility |
| --- | --- |
| `Elevate-Autocoach-Design-Library.md` | Exact tokens, shared component states, typography, charts, native semantics, accessibility, and release requirements |
| `DESIGN_SYSTEM.md` | Product-specific behavior, data scope, navigation, and the shared-width override |
| `dist/styles.css` | Application layout in the legacy composition layer |
| `dist/design-system.css` | Sole literal root, shared components, and accessibility rules, loaded after layout styles |
| `dist/design-system.js` | Shared icons and accessible help |
| `dist/components.js` | Shared KPI, status dictionary, table, and native-control components; `uiSessionState` returns the single record state, with "In progress" for automated sessions waiting on the driver |
| `dist/charts.js` | Reusable chart marks, scales, legends, summaries, and equivalent tables |
| `dist/app.js` | Fixture data, ledger scopes, navigation, filtering, lifecycle actions, and the automation mode/cadence draft state; `renderSettings` re-renders the Programmes › Automation tab |
| `dist/overview.js` | Shared KPI and chart data bindings |
| `dist/session-evidence.js` | Event identity, source metadata, and driver/session associations |
| `dist/session-workspace.js` | Evidence review, sharing, conversation, per-session drafts, and `sessionOpenReason` (why automation opened an Overdue, Repeated or Replied one-on-one) |
| `dist/driver-spotlight.js` | Driver portfolio and its session return context |
| `dist/programs.js` | Full-page Programmes: tabs, programme comparison/event rates, mapped Learning, per-programme configuration, fleet-wide Automation and the Automation Centre programme table |
| `dist/program-activity.js` | Programme Activity composition and scoped weekly chart, attention records and retained outcome data; explicit sample-history provenance |
| `dist/program-drawer.js` | Retired, unloaded program-drawer reference; its source data now appears in `dist/programs.js` |
| `tests/design-library.test.mjs` | Static design-library acceptance and palette contrast |
| `tests/design-library-browser.mjs` | Shared states, native semantics, KPI geometry, charts, accessibility, and responsive acceptance |
| `tests/browser-smoke.mjs` | Existing navigation, filtering, data, lifecycle, and route regression checks |
| `tests/programmes-workspace.mjs` | Six-destination navigation, mobile More, merged tab scope, all ten programmes across 1/4/8 weeks, sample-history provenance, attention and record returns |

Page-specific code may arrange content and bind data. It must not redefine shared tokens, typography, KPI tiles, table cells, status colors, focus, or selection states. Dynamic data widths are allowed; inline colors are not.

## Visual and semantic baseline

Apply the complete library, including its appendix. Every root token is literal. Ember orange `#B84A00` identifies actions and location; petrol `#236C72` identifies neutral data. Headline numbers remain ink. Status and directional colors are independent from brand and categorical series. Use the native system font stack and the six rem-based type steps, reduced to 85% by the dated user amendment. At the unchanged 16px HTML root, body/control text is 11.9px and section/drawer headings are 13.6px. Keep spacing, hit targets, chart geometry and drawer widths unchanged. The amended neutrals are canvas `#FAFBFD`, soft `#FCFDFE`, strong `#F3F5F7`, border `#DEE4EB`, and separator `#EEF1F5`; segmented containers are white. Permanent surfaces have borders and no shadows; only overlays float.

Each reporting summary uses one `.kpi-strip`, with `.kpi-tile` children on a single nonwrapping row. The seven-value Programmes strip belongs to Activity; its other tabs do not repeat those totals. Activity session-count shortcuts retain programme/period scope; Needs review focuses the attention panel joined to the chart. Learning, Configuration and Automation do not display irrelevant reporting KPIs. Narrow strips scroll locally with a label and keyboard access. Use label with accessible info hint → value → optional useful change → optional meaningful meter. Put explanatory scope/definitions in the hint; omit empty context rows. Do not invent denominators or zeroes to fill a tile.

Use real navigation links, native radio groups in fieldsets, native tables with header cells, labelled inputs, and native modal dialogs. Declare table alignment once per column: text/status columns align left and quantities align right. The shared renderer propagates that declaration to both headers and every body/footer cell; do not style a numeric header independently from its cells. Sort controls and wrapped table links inherit the same alignment, and number inputs align their entered values right. The shared `.data-table button.text-link { text-align: inherit; }` rule keeps wrapped group names aligned with their column. Numeric columns sort by signed quantity, with missing values last in both directions; cells with descriptive values such as “4% fewer” carry a numeric `data-sort-value`. This includes chart data equivalents, the Programmes Configuration and Automation editors, and all drawer tables. View tabs use the shared underline selection and manual keyboard activation: arrow keys/Home/End move focus; Enter/Space activates. Navigation uses an edge marker; radio segments use a bounded selected cell. Focus overlays selection rather than replacing it.

Session tables show one **State** column, and a record shows exactly one of **In progress, Replied, Overdue, Completed** or **Repeated**; Archived remains as history. Overdue, Repeated and Replied are the three states a person must act on; an automated session waiting on the driver reads In progress. "Needs review" is not a record state or a lifecycle word: it is only the name of the queue that groups Overdue, Repeated and Replied in KPI tiles, the Sessions tab and the "Needs you this week" card. Nothing is labelled "Awaiting driver", "Awaiting session" or "Session needed". Shared status words and icons come from one dictionary. Completed clears obsolete attention. Opening an Overdue, Repeated or Replied session shows one line under the header explaining why automation opened the one-on-one (`sessionOpenReason`). There is no Method column; **Coach** reads "Automated" or the manager's name. Dates are **Started**, **Completed**, and **Due**, with an em dash where a record has none; there is no Updated column. Delivery problems retry automatically; do not restore a Blocked workflow.

Charts use title → concise context → legend → plot → collapsed **Summary and data**. Put long source/exposure notes inside that disclosure. Use one petrol colour for Automation/Attention Centre category dots and bars; retain the declared categorical series in other charts. Before/after charts use adjacent grey/petrol horizontal bars with a common zero baseline and a separate signed change. Their shared geometry scales with user text zoom so labels and values remain separate at 200%; preserve normal-size geometry and local plot scrolling. Weekly activity uses petrol/umber bars and a cased ink score line on a fixed 0–100 axis. Preserve exact values, source windows, and equivalent tables; a before/after observation does not establish causation.

## Workspace navigation

Primary navigation has six desktop destinations: **Automation Centre, Sessions, Programmes, Drivers, Training library and Driver app**. At **680px and below**, the bottom bar keeps Automation Centre, Sessions, Programmes and Drivers, with **More** exposing Training library and Driver app. Driver app is a required clickable destination, not a route-only page; preserve the user’s KEEP markers in `dist/index.html` and the instructions in `AGENTS.md`. Programmes replaces the separate Analytics and Programs entries. Its tabs are **Activity, Learning, Configuration and Automation**. Drivers contains Drivers and Groups tabs. The standalone Learning destination remains the full lesson library, while Programmes › Learning shows programme mappings. The Settings page and global search remain removed; fleet-wide mode, cadence and completion policy stay in Programmes › Automation. Preserve the linked Driver app; it is separate from the retired preview control. Do not restore the fleet switcher or account footer.

Automation Centre remains the landing page with independent programme/time scope, its three grouped Overdue, Repeated and Replied rows, Automation this week and the compact programme table. It has no featured driver. Its programme links open Programmes and its automation status link opens Programmes › Automation. Sessions remains the full searchable ledger; its Needs review queue and state filters are the review workflow.

Programmes opens **All programmes / Activity**. The page title stays Programmes and the selector identifies its scope. One strip shows **Identified, In progress, Needs review, Completed, Automated sessions, One-on-one sessions, Elevate score**; selecting a programme names the last value **`<programme name> score`**, matching the weekly-chart score label. The lifecycle four reconcile separately from the two total method counts. Completed help retains its fraction/percentage and meaningful meter. Score is an independent recorded prototype measure, not a sum or average of the coaching counts.

Activity orders **joined weekly coaching/attention row → All-programme comparison, when in All scope → collapsed outcomes**. Activity joins **weekly coaching at 70% and drivers needing attention at 30%** into one desktop row with **zero gutter and a single divider**. The full attention list scrolls independently; its count sits inline to the right of the heading. The joined panels share their top and bottom edges and stack on narrow screens. The selected programme’s KPI and weekly-chart score label is **`<programme name> score`** (for example, “Speeding score”); All programmes keeps **“Elevate score”**. The separate selected-programme event-rate Trend/Comparison card is removed. All programmes retains its Coaching/Event rates comparison and the underlying recorded rates, source endpoints and equivalent data. Attention uses the same selected programme/period ledger and opens the relevant driver session. Needs review focuses this section. All-programme comparison retains one native Coaching/Event rates view switch, every programme including zero-coaching entries, source counts, signed rate change and recorded score. The full programme list is not duplicated in two simultaneously visible comparison views. Selected programmes retain weekly coaching and recorded outcomes without a second event-rate chart. Sessions remains the only full session ledger; count shortcuts and attention actions preserve programme, period and state when entering it or its session drawer.

The weekly chart distinguishes current source counts from history. Fleet weekly snapshots retain their overall score series; coaching counts and score remain separate measures. User-authorized programme example histories carry a visible **Sample history** cue, explicit dates, an accessible description and equivalent data; their historical shapes must not be represented as observed session-ledger history or a computed score model. The current endpoint reads This week ledger counts even when a longer reporting window is selected. This week shows eight weeks of chart context; Last 4/8 weeks shows four/eight snapshots, with explicit chart dates distinct from page totals. Unknown or newly created programme history is unavailable rather than copied from another programme. Event-rate comparisons preserve units and exact endpoints; do not average programme rates into a fleet rate without exposure.

Recorded outcomes remains initially collapsed. Its undated eligible/improved/repeated sample retains the visible unavailable observation window and independence from the reporting period; missing samples have no borrowed fallback. Long methodology stays inside the disclosure and each chart keeps its own Summary and data. No sample establishes a causal coaching effect.

Groups shares Programme and Period controls with Activity. These scope coaching workload and related session drilldowns. Group size and **Overall Elevate score** retain their recorded group-wide scope and must be labelled accordingly; do not silently reinterpret them as programme scores or period-specific outcomes. Group → programme → session navigation preserves source scope, scroll and opener. Opening a programme from a group does not imply a group-only programme ledger unless that scope is explicitly applied.

Learning and Configuration retain the Programme selector but omit coaching KPIs, Period and date captions because mappings and configuration are not period-scoped. Automation is fleet-wide, shows **Applies to every programme** and omits the programme selector. Preserve `coachingPeriod` when moving through these tabs. Per-programme Configuration still supports local creation/deletion, rule source/severity/weight/threshold, coaching threshold, escalation and coach routing. Fleet-wide Automation retains its draft, preview, discard and activation flow; the merge does not alter either behavior. Manual one-on-ones remain an individual Sessions action.

Canonical routes retain implementation-compatible names: `#programs` opens Activity; `programTab=content|configuration|automation` selects the other tabs; `#drivers` opens the independent directory and `driversTab=groups` selects Groups; `#learning` opens the full library. `programComparison=rates` restores the all-programme Event rates view. Legacy Analytics/Outcomes/Activity routes map to Activity, `analytics=groups` and legacy `programTab=groups` to Drivers / Groups, and `analytics=drivers` to standalone Drivers. Legacy `programTab=overview` maps to Activity and `#settings` maps to Automation; `#content` maps to Learning. Preserve programme/time, search, driver/group filters and linked `driver`/`record` context while canonicalizing URLs.

Only sessions, driver portfolios and groups use record drawers. Programme details remain a full page, with no Quick view or loaded programme-modal renderer. Back and close restore the exact source tab, programme, period, filters, scroll and invoker, including cross-workspace Groups and Activity sources. Legacy `programPreview` and `programStage` links still resolve to full-page scope and linked sessions. Top-level Programmes navigation resets to All programmes / Activity; browser history restores prior scope. Driver portfolio and session routing remain independent.

Desktop navigation expands to 224px or collapses to 72px. The toggle preserves focus, reports `aria-expanded`, and remembers the preference locally with a storage-unavailable fallback. Collapsed icons expose names on hover and focus. At 681–900px expansion overlays the workspace; at 680px and below the bottom bar exposes More with Learning and Driver app. Returning to desktop restores the saved preference.

## Control placement

Use page header → one KPI strip → dataset toolbar → applied filters → primary content → supporting detail. Programmes tabs are Activity, Learning, Configuration and Automation; Drivers owns Groups. Activity owns its seven-KPI strip; Activity and Groups share programme/period reporting controls. Learning and Configuration show only relevant programme scope, and Automation is fleet-wide; other page compositions remain as specified. The header places the title left and actions right. Put view tabs left in the dataset toolbar; keep applicable program/period, search and Filters together on the right, with search immediately before Filters. Reporting-only scope replaces unsupported search or filters. Controls for a nested dataset stay on the right of that dataset's heading. On smaller screens the control group wraps beneath the views and retains its order; do not scatter individual filters across both sides. All controls retain accessible names, and search stays mounted as results change.

The September 7 cleanup uses one stable Programmes title, a named Programme selector where applicable and one exact page-period caption on Activity and Groups. Learning and Configuration retain programme scope without time controls; Automation identifies its fleet-wide scope. Do not repeat the period in every section, copy a KPI status into the page title, or add generic cross-links to destinations already present in navigation. Preserve context that changes interpretation: undated samples, chart endpoints, rate units, fleet versus directory scope, and total versus active methods. State/coach options carry counts; the filtered result count is announced without a second visible heading count. Hide pagination controls when there is only one page.

Use 24px between major sections, 16px between a section heading and its content, and 8px between related controls. The joined 70/30 Activity panels are one composition: their internal gutter is zero, with one divider instead of two separated card borders. Segmented tracks have 4px padding while options retain their 36px minimum height. Charts align their disclosure with the chart content and have no inherited side or bottom margins. A panel owns its internal gap; legend and disclosure margins must not add a second gap. Small driver highlights size to their content instead of stretching to match a neighboring chart.

Programmes Automation uses available container width: Mode and Cadence share a row from 44rem, and Completion joins them on the right from 64rem. Below those widths the sections stack in their original reading order. Keep the shared 24px column gap, align section headings and controls, and let options and footer actions wrap instead of squeezing their targets. The active-status line and draft/save actions span the layout; changing its arrangement does not change the draft, preview, discard or activation flow.

Two-column fact tables fit their container and wrap long values; they do not inherit the 48rem minimum used for wide record tables. They retain shared cell spacing, header alignment and native table semantics.

Filters open on request and show an applied count when closed. Reset is in the panel footer left; Done is right. Escape, backdrop, and Done dismiss the panel and restore its invoker. Native modal state must be released during navigation. Announce results through one restrained live region.

Manual coaching remains a deliberate individual action because automated coaching is the ordinary workflow. On Programmes › Automation, Save and activate and Discard changes stay with the dirty draft and are disabled when nothing has changed. Fleet-wide automation changes share the draft → Run preview → Save and activate flow; Run preview sends nothing, and cancelling or discarding must not modify active configuration. Per-program Configuration edits (rules, threshold, escalation, coach) save immediately to local storage and do not use the draft flow.

## Shared reporting period and data

Automation Centre, Sessions and the reporting tabs in Programmes share `data-coaching-period` and the `period` URL value: This week, Last 4 weeks, Last 8 weeks. `currentCycleCounts(period, programId)`, `coachingCounts(predicate, period)`, `rateChange()` and `periodScopeLabel()` provide the common scope. Counts, attention, comparisons and session shortcuts must reconcile to that selected programme/window. Automation Centre programme selection remains independent of the Programmes workspace selection and of time.

Use the current ledger rather than historical hard-coded totals. **Identified = In progress + Needs review + Completed**, with archived completions counted in Completed. **Actual session total = Automated sessions + One-on-one sessions**. Method totals include in-progress, review and completed records; **Automated in progress** and **One-on-one in progress** identify only the active subset. Needs review contains Overdue, Repeated and Replied actual sessions. The current interface has no pending Session needed state; older pending-flag arithmetic in historical review notes must not govern the merged interface.

The seventh Activity KPI is the recorded overall Elevate score for All programmes or the selected recorded prototype **`<programme name> score`**. Use the same scope-specific score name in the KPI and weekly chart. Both use the 0–100 scale, higher is safer; neither is calculated from the selected coaching counts, and the weighted programme roll-up is not configured. Scores without source values stay unavailable. Weekly chart examples and undated outcome samples are explicitly distinct from current ledger totals. Programme sample histories may illustrate the approved design but must be labelled in visible context, accessible descriptions and equivalent data. Fleet score cannot silently become a programme score.

Summaries keep programme/time scope while search, state and coach filters narrow their own tables. A summary shortcut retains scope and clears conflicting table filters before opening its named stage or method. Driver-directory filters do not alter all-programme totals. Groups scopes coaching workload by programme/period while labelling recorded group scores as overall. Tests reconcile against current source records, including locally created/completed records, rather than preserving obsolete fixture numbers. Automation previews still estimate no driver volume without source observations.

Event-rate changes use the latest week against its previous week for a one-week span, otherwise against the first week of the displayed span. Longer-period Automation Centre deltas say **No prior window** when no comparable prior period is supplied. Keep events per 1,000 trips distinct from events per 100,000 trips; never average rates without exposure.

The safety distribution describes 1,024 fixture drivers; the directory contains 14 representative records. Include unscored drivers in the fleet distribution. Top score improvements lists the five largest recorded increases among those directory records as full-width rows with a gain bar, Taylor Brooks first at +7 points, 71 → 78; it is not a fleet ranking or a dated weekly score change. Coaching by group belongs to Groups. Session counts are not unique-driver counts.

## Record drawers

**Explicit product override:** all overlay record drawers use the same `--drawer-width: 1060px`, capped by `calc(100vw - var(--drawer-gutter))` with `--drawer-gutter: 84px`; at 680px and below they use the full viewport width. This preserves the user's requirement that every drawer have identical width. It overrides only the illustrative `32rem` width in the library's `.dialog.drawer` CSS example. Groups, driver portfolios and sessions all follow this rule. Programmes is a full page and is not an overlay. Nested record navigation never changes drawer width.

Use the library's shared `.drawer__header`, native modal dialog behavior, neutral surfaces, and overlay shadow. Move focus inside on opening, contain it while modal, close on Escape/backdrop/Close, and restore the exact invoker. The background is inert. Content scrolls inside the drawer; tables, plots, and KPI strips may scroll in their named local regions.

### Driver portfolios

The driver name in the Drivers directory opens a portfolio. Its separate **Action** column offers **View session** for an actual active session (prefer the displayed focus), or **Create session** when no active session exists, including drivers with only completed/archived history. Past sessions remain accessible through the portfolio. Creation uses the existing prefilled form and Cancel changes nothing. Inside a portfolio, **Open session** enters coaching. Driver identity appears once in the header. Keep the recorded latest score distinct from the observed driving week. Weekly miles, trips, and days driven come from dated sample observations; missing coverage is unavailable, not zero. Do not invent a weekly score series.

Use the current product arrangement documented in [Driver spotlight](docs/driver-spotlight.md), while applying the library's shared KPI, chart, table, header, and control classes. Deduplicate source evidence by event ID. One multicamera incident is one evidence record, as is one pattern spanning multiple trips. Exclude dismissed events and delivery failures from rule counts; retain dismissed evidence as labelled history where appropriate. Historical rule evaluations need their original rule/version/context, not current configuration.

Opening a session preserves coaching scope, expanded exception, active camera, and scroll. Back to driver restores that context. The `driver` URL identifies a portfolio; `record` identifies a session. Closing preserves directory search and filters.

### Session evidence and conversation

Use compact Events / Videos counts with no Messages KPI, neutral conversation bubbles with author labels, and the shared evidence header. Retain the useful video/map pairing and compact composer; do not recreate a permanent repeated metadata rail, duplicate history timeline, or attachment strip.

Stable events own camera clips. Preview changes the viewer only. Selecting an event stages all its clips; patterns remain data with zero video clips. One browser provides This driver and Unassigned scopes, visible search, checkboxes, and preview. Use selection commits the draft; Cancel/Escape/backdrop discards browser changes.

Only Send shares evidence and assigns unassigned events. Preserve original category, source, type, timestamp, vehicle, and location. Cross-driver or closed-session assignment is rejected. An attachments-only reply is valid. First drafts select linked evidence not already shared; Send clears selection, and reopening does not reselect it.

Reply and private-note text, carets, active event/camera, and selection belong to each session. Evidence refreshes preserve the textarea. Private notes are team-visible, hide sharing controls, retain the separate reply selection, and never assign evidence. These prototype drafts persist in memory, not across reloads.

Completed and archived sessions have no composer or Add events action. Archive/Restore retain history; Restore returns to Completed. Failed deliveries follow automatic retry under the active automated workflow, without a new Blocked state or manual account-relink gate.

Actual supplied media URLs use native playback; valid supplied coordinates place the incident map. Missing media/GPS uses explicitly labelled illustrations without an invented incident pin. Do not infer routes or generate incident facts from session hashes. See [Session workspace](docs/session-redesign.md).

## Validation and future changes

Run the documented commands in [README](README.md), including design-library acceptance for shared changes. Follow [migration and release checks](docs/design-library-migration.md). Verify the shared state specimen and representative pages at desktop, narrow widths, 200% text zoom, reduced motion, and forced colors. Contrast-pair tests do not establish whole-product WCAG conformance; report the checks actually completed and any remaining limitations.

**Historical validation — earlier September 7 merge, before this navigation/layout correction:** `npm run check`, all 15 static tests, `test:workspace`, `test:programs`, `test:program-drawer`, `test:browser` (including metric reconciliation), `test:drivers`, `test:sessions`, `test:config`, `test:driver`, `test:design` and `test:cleanup`. Coverage includes all ten programmes at 1/4/8 weeks against the raw ledger; unique drivers, attention and return focus; sample-history reconciliation; direct Driver app updates reflected in Programmes/Drivers; and preserved configuration and session behavior. Shared alignment checked 126 tables, 2,860 cells, 24 numeric inputs and 486 sort headers; responsive design covered 320/390/1440px and 200% text zoom, reduced motion and forced colors. Those earlier commands passed; the latest six-destination/joined-panel correction requires its own recorded run. These local prototype checks do not validate live ingestion, a production scoring engine, causal impact or full WCAG conformance. See [current acceptance](docs/programs-review.md#current-acceptance-requirements) for the evidence boundaries.

## Driver app surface

The [driver app](docs/driver-app.md) (`dist/driver/`) is a separate, driver-facing iOS surface implemented from the imported design `docs/design-import/Elevate.dc.html`. It intentionally does not use the manager library's tokens, chrome or type: ink green with one warm accent, Bricolage Grotesque and Instrument Sans, 20px radii, dark surfaces only for the score and in-motion screens. The manager library still governs everything in `dist/index.html`, including the required **Driver app** sidebar and mobile More destination (`#driver-app`), which renders the app alone in a full-height frame. The standalone `/driver/` route also remains available. `dist/driver-link.js` registers that destination and keeps the two surfaces in step through local storage without editing `app.js`.

## Chat amendment — 14 September 2026 (UI polish, branch calvin-ui-polish-5.1)

A screen-by-screen review of the 5.0 build against reference patterns (Mobbin: Shopify Orders, StackAI, incident.io, Navattic, Twingate; Bloom, Visible, Monzo, Acorns, HoneyBook on iOS) produced these shared-rule changes. Each keeps the version 1.1 library, the five session states and the single-row KPI strip.

- **KPI tile anatomy is the same on every strip.** `uiKpi` accepts an optional `trend` and renders it under the value as `.kpi-trend` (arrow, signed magnitude, comparison), the markup the Automation Centre strip already used. `uiSessionCycleTrends` supplies week-over-week deltas for Identified, In progress, Needs review and Completed on the Sessions strip and the All programmes strip; a selected programme or a multi-week span shows “No prior window”, mirroring the Automation Centre.
- **KPI tile floor stays at 11rem (175px).** Narrowing tiles to fit seven on one 1440px row was tried and reverted: the design-library acceptance suite asserts a readable 175px minimum, so a strip wider than the workspace keeps scrolling horizontally rather than shrinking or wrapping.
- **Summary and data reads as a control.** The chart disclosure summary carries a chevron that rotates when open, the default list marker is hidden, and the label keeps the exact text the acceptance suite asserts.
- **Session drawer.** The category breakdown opens by default so the evidence column is not empty on first open. An empty conversation offers three editable reply starters (`.sw-starter`) for one-on-one sessions only; choosing one fills the reply draft through the existing composer state. Automated, completed and archived sessions keep the plain “No messages yet” line.
- **Automation Centre.** The programme context link (“Explore programs” / “Open program”) moved out of its own row into the Programs section heading beside “All programs”, in a `.program-section-actions` group.
- **Favicon.** `dist/index.html` embeds the brand steps mark as an SVG data URI in `--primary`; no request is made for `/favicon.ico`.
- **Links keep their underline.** The review considered hover-only underlines on driver and programme names and rejected it: the library rule that colour alone must not imply clickability stands.
- **Driver app.** Real tab-bar glyphs, a brand-coloured focus ring on programmatically focused headings, an eight-week score sparkline with the fleet-target rule, a stepped lesson list on Learn, a session progress step list on Coaching, and per-programme trend arrows on Home where the data carries a previous score. See [docs/driver-app.md](docs/driver-app.md).
