# Repository instructions

For every frontend or UI/UX task, read [Elevate-Autocoach-Design-Library.md](Elevate-Autocoach-Design-Library.md) first, then [DESIGN_SYSTEM.md](DESIGN_SYSTEM.md). The September 7, 2026 version 1.1 library is the governing visual and accessibility specification. The local design contract adds product behavior, data definitions, and the explicit shared drawer-width override. Its dated amendment governs brighter neutrals, the 85% text scale, petrol attention-category marks, and progressive disclosure of KPI/chart explanations. The [Programmes review scope](docs/programs-review.md) records the latest September 7 navigation merge and its data boundaries. Current user instructions take precedence.

Use the shared tokens and components in `dist/design-system.css` and the icon/help behavior in `dist/design-system.js`. Reuse the KPI, status, table, and native-control helpers in `dist/components.js`, and the chart primitives in `dist/charts.js`. Do not restore the retired blue palette, brand-colored data, decorative gradients, downloaded fonts, root token aliases, page-specific KPI/table variants, or custom non-native navigation and form semantics. Change a shared rule in the canonical implementation and document the decision together.

This is a static prototype served from `dist/`. Preserve existing user edits and coordinate file ownership during overlapping work. Validation commands, browser setup, and release checks are in [README.md](README.md) and [docs/design-library-migration.md](docs/design-library-migration.md).

The latest navigation is Automation Centre, Sessions, Programmes, Drivers, Training library and **Driver app**, plus the matching More entry on narrow screens. **The Driver app sidebar item and its mobile More entry are REQUIRED by the product owner (Calvin, Sep 7 2026) and must not be removed or reduced to a route-only page.** It is a clickable sixth destination that opens the driver-facing iOS app (`#view-driver`, registered in `dist/driver-link.js`); the standalone app and direct `#driver-app` route also remain available. If you believe it should change, ask Calvin first; do not delete the nav item or the `<!-- KEEP: Driver app ... -->` markers in `dist/index.html`. See [docs/driver-app.md](docs/driver-app.md). Programmes replaces the separate Analytics and Programs entries and contains Activity, Learning, Configuration and Automation. Drivers is a standalone destination with Drivers and Groups tabs. Groups no longer belongs to Programmes. Programmes starts at All programmes / Activity: seven current summary values, weekly chart 70% and independently scrolling attention 30% side by side (stacked on narrow screens), comparison or event rates, then collapsed outcomes. Label sample programme histories; do not present them as recorded ledger history. Preserve programme/period scope, legacy routes, driver/group/session drawers and exact return context. Programme details live on the full page; do not restore a programme drawer or Quick view. Configuration and fleet-wide Automation behavior remain intact.

The September 7 navigation merge passed the syntax/static checks, merged-workspace and source-ledger suites, configuration/session/driver regressions, and shared design/cleanup acceptance. Run `npm run test:workspace` for programme scope and history provenance. The [current acceptance record](docs/programs-review.md#current-acceptance-requirements) gives tested commands, scope and prototype limits; preserve its historical boundary.

The [full chat audit](docs/request-audit-2026-09-07.md) governs completeness claims. Do not treat older passing suites or generated meeting notes as evidence that every user requirement was implemented. Programme scoring and ladder evaluation await the specific open decisions; do not invent production behavior.
