# Programmes review prototype — September 7, 2026

> Latest correction: six desktop destinations include the required Driver app link; mobile More contains Learning and Driver app. Groups lives under Drivers; Programmes has Activity, Learning, Configuration and Automation. Weekly coaching and attention join in a 70/30 row with zero gutter, one divider and an inline attention count. Selected programme score labels use the programme name, and its separate event-rate card is removed; All-programme comparison and recorded data remain. This document contains earlier scope/history; the [full request audit](request-audit-2026-09-07.md) records current implementation and outstanding decisions. Historical descriptions of scoring or escalation are not proof of an operational engine.


The current contract keeps **Programmes** and **Drivers standalone**, with Groups under Drivers and the required Driver app destination. The joined Activity composition and data rules below supersede earlier September 7 layouts. Earlier passing results remain explicitly historical; they do not establish validation of the latest correction.

## Current interface

Primary navigation has six desktop destinations: **Automation Centre, Sessions, Programmes, Drivers, Learning and Driver app**. At **680px and below**, the bottom bar keeps Automation Centre, Sessions, Programmes and Drivers; **More** contains Learning and Driver app. Preserve the required Driver app entry and the user’s KEEP markers in `dist/index.html` and `AGENTS.md`; the app must not become route-only. Programmes contains **Activity, Learning, Configuration and Automation**. Drivers contains **Drivers / Groups**. Programme Learning shows mappings; standalone Learning is the full library. Driver app links open `#driver-app`; direct access and `/driver/` remain available.

**All programmes / Activity** is the default. Activity alone uses the seven-value strip: **Identified, In progress, Needs review, Completed, Automated sessions, One-on-one sessions, Elevate score**. Selected programmes name the final value **`<programme name> score`**, for example **Speeding score**, matching the weekly-chart score label. All programmes keeps **Elevate score**. The current ledger supplies the lifecycle and method counts. Identified reconciles to In progress + Needs review + Completed; method totals include every actual in-progress, review and completed session. Explicit active-method labels distinguish those subsets. Needs review contains Overdue, Repeated and Replied sessions; no pending Session needed state is restored. Completed help retains the fraction, percentage and meaningful meter, with no percentage or meter when there is no denominator. Overall and programme scores remain independent recorded prototype values, not computed from coaching counts.

Activity follows **joined weekly coaching/attention row → All-programme comparison when applicable → collapsed outcomes**. Activity uses joined **70% weekly coaching / 30% drivers needing attention** panels with **zero gutter and a single divider**. The full attention list scrolls independently, and its count sits inline to the right of the heading. Panels share their top and bottom edges and stack on narrow screens. Attention uses the same programme/period ledger. Its driver/session actions open the relevant record; Needs review focuses this section. Other count shortcuts open Sessions with programme, period, state and method scope. The complete coaching ledger stays in Sessions.

All programmes retains one **Coaching / Event rates** comparison module. Its table preserves every programme, including zero-coaching entries, and shows Needs review, In progress, Completed, total Automated sessions, total One-on-one sessions, signed Event-rate change and recorded Score. Event rates shows the retained full Before/After chart in that same module. The full list is not duplicated across simultaneous table and chart views. Selecting a programme keeps weekly coaching and attention/outcomes access but removes the separate event-rate Trend/Comparison card. The underlying recorded rates remain in the All-programme comparison, Event rates view and equivalent data. Units, observation endpoints, shared numeric alignment, sorting and equivalent data remain available.

Weekly history has explicit provenance. Fleet weekly snapshots remain fleet data. User-authorized programme example histories must show **Sample history**, dated observations and the same caveat in accessible descriptions and equivalent data. Their current endpoint uses This week ledger counts even for a longer reporting window; earlier sample shapes are not claimed as observed ledger history or evidence of a scoring engine. This week retains eight weeks of explicitly dated context, while Last 4/8 weeks shows four/eight snapshots. Page totals and attention still follow the selected reporting window. Newly created or unsupported programme histories stay unavailable. The overall Elevate score is independent from programme-specific sample curves and unconfigured weighted roll-ups.

**Recorded outcomes** remains one initially collapsed disclosure. Its eligible/improved/repeated source facts have an **Undated sample** cue and an expanded statement that the observation window is unavailable and independent of reporting period. Three separate driver observations also retain their recorded Before/After/change/completion fields where applicable; missing dates, exposure and completion denominators remain explicit. They are not the selected period’s session completion rate. No source sample is borrowed from another programme. Methodology stays with these facts, and every chart keeps its own Summary and data. Event-rate movement is not a causal coaching effect; programme rates are not averaged into a fleet rate without exposure.

The page title stays **Programmes**. Tabs stay left; applicable controls stay together right. Activity and Groups share Programme, Period and one exact date caption. Learning and Configuration retain Programme scope but omit Activity totals and time controls. Automation reads **Applies to every programme** and has no programme selector. Preserve the reporting period when moving between tabs.

Groups applies programme/time scope to coaching workload and session drilldowns. Group size and **Overall Elevate score** retain their recorded overall scope, labelled explicitly. Driver directory filtering uses matching programme records and keeps its overall-score semantics. Names open portfolios, explicit session actions open the relevant scoped session, and session return restores the directory context.

Configuration retains the existing local create/delete programme flow; rule source, severity, weight, threshold and enabled/direct-handoff settings; coaching threshold; escalation; and coach routing. Fleet-wide Automation keeps its draft/preview/discard/save-and-activate behavior. This navigation merge does not alter either flow or implement a production scoring, ingestion or execution engine. Manual one-on-ones remain a Sessions action.

## Navigation and retained context

Programme links from Automation Centre, Groups and former reports open the full Programmes page. No separate programme modal or Quick view is loaded. Driver, Group and Session records retain the uniform 1060px drawer contract, responsive cap, mobile full-width behavior and evidence/conversation workspace.

Back restores the originating tab, programme, period, filters, scroll and source control, including Group sources under Drivers and Activity sources under Programmes. A group breadcrumb alone does not make a programme ledger group-only; expose its all-group scope when necessary. Session close/Back restores its source context, and driver-origin sessions retain Back to driver. Driver name and session actions remain separate routes.

Canonical routes preserve existing implementation keys: `#programs` defaults to Activity; `programTab=content|configuration|automation` selects another tab. `#drivers` is standalone and `driversTab=groups` selects Groups and `#learning` is the full library. `programComparison=rates` preserves the all-programme Event rates view; top-level Programmes navigation resets to All programmes / Activity, while history restores the prior scope. Programme selection and reporting period are independent.

Legacy `#analytics`, `#outcomes` and `#activity` links resolve to Activity; `analytics=groups` and legacy `programTab=groups` resolve to Drivers / Groups and `analytics=drivers` to standalone Drivers. Legacy `programTab=overview` resolves to Activity, `#settings` to Automation and `#content` to Learning. Preserve programme, period, directory search/status/group/score/sort filters and linked `driver`/`record` context during canonicalization. Legacy `programPreview` and `programStage` still migrate to full-page programme scope and linked sessions. Obsolete `programState` and `programCoach` are not the current page state; Sessions owns its own state/coach scope.

## Review scope and source limits

This extends the [Kevin/Calvin call review](kevin-calvin-call-2026-09-07.md) and [domain review](program-domain-review.md). The current work consolidates navigation and retained data access. Existing local configuration and the linked driver-facing flow remain working prototype behavior; no live fleet integration or production scoring/LMS backend is implied.

| Measure or interaction | Evidence and presentation rule |
| --- | --- |
| Coaching stages and method totals | Current selected-period session ledger. Keep total methods distinct from active methods and preserve actual record actions. |
| Needs review | Current Overdue, Repeated and Replied sessions in the selected programme/window; display in the attention panel joined to the weekly Activity chart. |
| Fleet weekly activity | Existing explicitly dated prototype weekly snapshots; current endpoint reconciles to the ledger. |
| Programme weekly activity | Labelled sample history authorized for the design. Distinguish earlier examples from current recorded counts; do not fabricate a source history for newly created programmes. |
| Programme event rates | Existing fixture observations with units and exact endpoints, not causal coaching impact. |
| `<programme name> score` | Programme-named KPI and weekly-chart series; recorded prototype 0–100 fixture where supplied; not calculated from current rule weights, and missing scores remain unavailable. |
| Overall Elevate score | Independent recorded overall fixture. The weighted programme roll-up is not configured. |
| Group score and coaching workload | Group score/size remain overall; selected programme and period narrow coaching workload only. |
| Recorded outcomes | Available eligible/improved/repeated sample is undated and independent of reporting period. No borrowed or aggregated programme samples. |
| Watch/acknowledgement, video outcomes and duration | Linked driver-app actions support their existing local flow; incomplete historical telemetry does not establish a verified programme cohort or causal video impact. |
| Configuration | Existing locally persisted programme settings and fleet automation draft flow; no live ingestion or rule-execution engine. |

The [metric definitions](metric-definitions.md) supply source reconciliation details; current records govern over obsolete fixed fixture arithmetic in historical reviews. “95% automated” in the call is a design principle, not permission to replace measured totals. Program ownership, quizzes and assignments remain deferred. Remaining domain work includes score normalization, exposure/missing rules, weighted roll-ups, true historical evaluations, enrolment/video versions and eligible follow-up cohorts. GCP configuration is unchanged and this work sends no transcript or messages to Jobin.

## Research informing the structure

The team reviewed the [Circle course screen](https://mobbin.com/screens/8137f10e-d943-44fd-ac18-8d417a131586) for a course title, compact summary, and student progress table, and [Deel's assignment screen](https://mobbin.com/screens/183566db-f0b6-42f8-b0e1-03e56e32ed35) for stage-based assignment views. These are structural references, not designs to copy wholesale.

Docebo's course report separates course totals from learner-level progress and training-material activity. This supports making one program the page context and putting its people below the summary. [Docebo course report documentation](https://help.docebo.com/hc/en-us/articles/360020080200-Checking-the-course-report)

TalentLMS describes a training matrix that relates individual users to course progress, completion, and outstanding work. That supports the next driver iteration: extend one existing coaching table to include every program, rather than add a second disconnected table. [TalentLMS reporting features](https://www.talentlms.com/features/lms-reporting)

## Current acceptance requirements

**Historical run — earlier September 7 navigation merge, before the latest correction:** `npm run check`, all 15 `npm test` static tests, `npm run test:workspace`, `npm run test:programs`, `npm run test:program-drawer`, `npm run test:browser` with metric reconciliation, `npm run test:drivers`, `npm run test:sessions`, `npm run test:config`, `npm run test:driver`, `npm run test:design` and `npm run test:cleanup`.

`test:workspace` reconciles all ten programmes across 1/4/8-week windows against the raw session ledger, including lifecycle/method totals, unique drivers, attention and session return context. It checks sample-history current endpoints and earlier programme-count sums against the existing fleet snapshots, preserving independent score semantics. Programme tests independently verify that Needs review focuses attention. Configuration and session regressions retain their existing behavior. The direct Driver app suite verifies linked updates and refreshes in both Programmes and Drivers.

That earlier shared design/cleanup acceptance covered the then-current five-destination navigation and five tabs, accessible scope controls, chart → attention order, retained outcomes, source-focus Back, native controls, drawer widths and responsive layouts at 320/390/1440px with 200% text zoom, reduced motion and forced colors. The shared alignment audit checked **126 tables, 2,860 cells, 24 numeric inputs and 486 sort headers**, including wrapped group-name links. Browser checks block external requests before navigating localhost.

Those listed commands passed against that earlier build. They do not assert a passing result for the latest six-destination navigation, zero-gutter join, dynamic score naming or selected-card removal. These historical results validate the local prototype and source relationships tested at that time. They do not verify live fleet ingestion, production rule execution, a computed weighted scoring model, historical LMS completeness, causal coaching impact or whole-product WCAG conformance. Earlier acceptance results below remain attributed to their earlier builds.

Required checks cover six primary destinations on desktop and, at 680px and below, four direct bottom-bar destinations plus More containing Learning and Driver app; standalone Drivers with Drivers / Groups; all four Programmes tabs; native manual keyboard activation, correctly labelled active panels and no duplicate navigation IDs. Verify legacy URL migration, canonical reload/history, programme/period independence, retained filters and driver/session source returns. Verify the required Driver app sidebar link and mobile More entry, direct route and linked flow; preserve the KEEP markers.

Reconcile current summary and attention counts for every programme and 1/4/8-week window against the ledger, including zero-coaching/new programmes. Verify total versus active method labels, completion denominators, independently scoped fleet/programme scores, source rate endpoints and unchanged undated samples. The weekly chart precedes attention in DOM order. On desktop they join at 70/30 with zero gutter and one divider, equal outer top/bottom edges, an independently scrolling attention list and the count inline to the right of its heading; on narrow screens they stack. Sample programme histories need visible/accessible/table provenance; no unlabelled fabricated history or implied fleet-score scoping is allowed. Groups workload follows programme/period while overall group score/size retain explicit scope.

Verify All-programme comparison/Event rates and underlying recorded data remain reachable, selected scope has no separate event-rate Trend/Comparison card, and both the KPI and weekly chart use `<programme name> score` while All keeps Elevate score; session and portfolio drawers retain their width, focus, source scroll and Back behavior. Check Configuration and Automation behavior without changing their semantics. Shared acceptance includes numeric header/cell alignment, sorting, labelled local scrolling, narrow layouts, 200% text zoom, reduced motion and forced colors. Browser checks block external requests before navigation.

Commands: `npm run check`, `npm test`, `npm run test:workspace`, `npm run test:programs`, `npm run test:program-drawer`, `npm run test:cleanup`, `npm run test:browser`, `npm run test:design`, `npm run test:drivers`, `npm run test:sessions`, `npm run test:config` and `npm run test:driver`. The retained program-drawer command verifies data retention and individual record navigation, not a programme modal. Report only commands actually run against this merge and any limitations.

Example canonical routes: `/?program=all#programs`, `/?program=speeding&period=4#programs`, `/?program=speeding&driversTab=groups#drivers`, `/?program=speeding&programTab=content#programs`, and `/?program=speeding&period=4#drivers`.

## Historical decisions and validation

These notes preserve earlier September 7 review builds and their recorded acceptance results. They are not the current interface contract and do not establish that the latest Programmes navigation merge has passed validation.

### Full-page data restoration before content consolidation

The user clarified that the requested restoration concerns **program data**, not a separate program drawer. This supersedes the earlier drawer-restoration interpretation below. Program links in Analytics, Groups, Automation Centre and search now open the full Programs page. Quick view is removed, and the former drawer script is no longer loaded. Driver, Group and Session drawers remain.

A selected Overview retains one Trend/Comparison chart and brings completion fraction/percentage, eligible/improved/repeated sample facts, clip indicators, review composition, active-method counts and one-on-one creation onto the page. No second chart or KPI strip is added. The driver sample is visibly undated and independent of reporting period; completion and coaching records remain period scoped. Pending flags keep Start session and actual records keep Open session.

Back returns to the source report/group with its scroll and opener. Session Back returns to the page and source record. New sessions created from Programs prefill that program and open over it, with refreshed active/manual filters; Cancel changes nothing. State/coach scope uses `programState` and `programCoach`; old `programPreview` / `programStage` links migrate to full-page scope and retain linked sessions. All programs, the three page tabs and Analytics Drivers/Groups remain unchanged.

Validation of this correction passed: 15 static tests, Programs acceptance, the repurposed `test:program-drawer` data-retention suite, shared design checks and browser smoke with metric reconciliation. Retained samples/completion and state/coach counts reconcile for all ten programs across 1/4/8 weeks. Legacy links, report/search/group returns, manual cancellation/creation, pending-flag conversion, exact session return focus/scroll, mobile widths and 200% text zoom are covered. The shared alignment audit covers 90 tables and 8,424 cells. The former drawer test filename is retained for command compatibility; it now verifies full-page data retention and individual-session drawers.

### Earlier review decisions (superseded where noted)

Automation Centre remains the landing page. Its program selector starts at **All programs**. Program scope and reporting period are independent: choosing Speeding must not silently change the date range, and changing the date range must not clear Speeding. Summary values, attention records, and their links describe the selected scope.

**Later user correction:** restore the familiar Analytics entry and program drawer workflow. Analytics opens Outcomes/Activity directly; program names in those reports, Groups and search open a quick-review drawer over the source workspace. Programs remains available for full-page review/configuration, with an explicit Open program page action from the drawer and Quick view from the page. Programs is additive. Drivers and Groups remain inside Analytics as tabs, alongside Outcomes and Activity, without separate sidebar entries. This supersedes the earlier removal of program drawers in the first Jobin mockup.

**All programs is the Programs landing view.** One summary strip shows Identified, In progress, Needs review, total Automated sessions, total One-on-one sessions and Completed. A full-width event-rate comparison and one program table compare all ten programs, including those with no current coaching. The table combines review/workload, method totals, completed counts, completion, latest event rate and period change. No fleet rate is inferred by averaging program rates. All underlying coaching records remain accessible in a collapsed disclosure; summary shortcuts open it at the requested state or coach.

Selecting a program in the filter or comparison table narrows the same page while preserving its reporting period. The only sections are **Overview, Content and Configuration**. Drivers stays inside Analytics; names in the Coaching table still open portfolios. All-program Content lists the mapped lesson metadata across programs. All-program Configuration provides current feed/content coverage and entry points into each program's draft configuration. This replaces the separate Programs Drivers tab.

A selected program places identity and scope first, then a concise summary, a visible event-rate graph, and one Coaching table. The graph defaults to **Trend**, showing all eight dated observations with its own scope label; **Comparison** switches the same card to Before/After for the selected reporting window. The Coaching table defaults to **All records**, retaining current work and completed/archived history; State and Coach filters remain optional. Preserve the distinction between records, actual sessions, and unique drivers. A program with no current coaching still exists and can be selected. Unavailable impact information is collapsed under **About program impact**.

Group, Driver, and Session records continue to use the same 1060px drawer contract. **Open session** is explicit in Program and Sessions Action columns, including completed and archived records; pending flags offer **Start session**. Closing returns to the originating list and action. Driver names still open the portfolio with its daily graph, and its session drilldown retains **Back to driver**. Program quick views use that same width. Program → session → Back preserves the selected stage, scroll and source record; Group → program → Back returns to the group. The underlying workspace stays in place. The Sessions evidence/conversation experience is retained.

### Earlier acceptance contract

`npm run test:programs` covers the new navigation and program switcher, shared program/time scope, source-count reconciliation, preserved record navigation, unavailable metrics, and responsive/native-control behavior. Existing design, browser, driver, and session coverage remains relevant; navigation assertions change only where the product contract intentionally changes. Browser checks block external requests before opening the local application.

The implemented routes include `/?program=speeding#automation`, `/?program=all#programs`, and `/?program=speeding&programTab=content#programs`. Opening `#programs` defaults to All programs / Overview. An independent `period=4` or `period=8` can be added. Program sections are `overview`, `content`, and `configuration`; legacy `programTab=drivers` resolves to Overview with its coaching records. Top-level Programs navigation returns to All programs / Overview, while browser history restores the previous selection. Driver/Group destinations are `?analytics=drivers#analytics` and `?analytics=groups#analytics`; former `#drivers` / `#groups` aliases still resolve.

The program page's state and coach filters operate on the existing source ledger. Content shows mapped lesson metadata; Configuration is a read-only proposal. The program quick-review drawer is restored; the obsolete generated assignment composer remains retired. Its stage tabs read actual session records and pending flags. A program-level one-on-one action preselects the program, including programs with no current coaching; Cancel makes no data changes. Creating a session from the program retains its context and opens that session.

The graph/drawer restoration retains the dated trend before Coaching, current and completed history, Trend/Comparison views, and active/completed/archived session actions. The weekly trend keeps its intrinsic plot width on narrow screens and expands with text zoom, with separate week labels and local scrolling. Analytics, driver portfolios and the session evidence/lifecycle suite passed before the All programs addition; the latest acceptance below records validation of that addition.

### Historical program-drawer interpretation (superseded)

The drawer contains one identified/stage KPI strip, a factual review shortcut, the weekly event-rate trend and one coaching region with All records, Needs review, Automated active, One-on-one active, Completed and Outcomes tabs. Driver names and Open session actions here both open the individual session. Driver portfolio navigation remains unchanged in Drivers.

Outcomes restores period-scoped completion and event-rate comparison, plus the old eligible-driver, improved-share and repeat sample. The sample is explicitly undated and does not change with the period selector; it must not be presented as a measured video cohort. No session records are manufactured to fill the view.

The URL values `programPreview`, `programStage` and optional `groupPreview` preserve review context alongside the existing `record`, `period`, filters and underlying route. Example: `/?analytics=activity&programPreview=following&programStage=completed&period=8#analytics`.

`npm run test:program-drawer` validates this restoration in addition to full-page Programs and shared UI tests. The raw call notes remain unchanged; this later user correction governs the restored UI.

Final additive-navigation acceptance passed: six sidebar destinations (Automation Centre, Sessions, Analytics, Programs, Content, Settings), with Outcomes, Activity, Drivers and Groups inside Analytics. Static checks, Programs, program-drawer, browser, Sessions, Drivers and shared design suites passed. The shared alignment audit covers 66 tables and 2,578 cells. Program/sample counts, nested drawer return, manual creation/cancellation, legacy links, mobile widths and 200% text zoom are covered.

### Earlier All programs acceptance

The All programs addition passes `npm run check`, all 15 static tests, `npm run test:programs`, `npm run test:program-drawer`, and `npm run test:design`. Source comparisons cover all ten programs at 1/4/8-week scopes, every stage/method total, event-rate endpoints, mapped lessons and feed settings. The focused Programs suite covers selector/row navigation, the three tabs, browser history, legacy Drivers-tab links, KPI disclosures and exact return focus for repeated driver names. All-program and selected-program pages pass at 1440/390/320px and 200% text zoom. Configuration remains read-only.

The shared audit now covers 84 tables and 7,920 cells. Numeric sorting checks verify signed changes against the source rates, decimals, formatted values, explicit sort quantities, editable number values and missing values last in both directions. The final editable-value fallback was checked with `tests/table-alignment.mjs --numeric-only` after the full shared suite. Program, group, driver and session drawer widths and retained navigation remain covered by the shared and program-drawer suites. Local browser requests were blocked from external services before navigation.
